class Tms implements TmsLoader {

	process ( collections: Pmd['tms'] ) {

		return new Promise(() => {}) as Promise<void>
	}

	postDefault () {

		return new Promise(() => {}) as Promise<void>
	}
}

export default Tms;
